# ner-detector scaffold and test coverage

**Archived dev note** (2026-05-24). Superseded by the public series post [Four ways to find "Pinecone" in a paragraph](../posts/04-picking-a-ner-backend.md).

## Done

- New **ner-detector** project: `pattern`, `transformers`, and `gliner` backends with CLI (`ner-detect` / `run.py`).
- Runtime profile **`config/ner.yaml`** for backend/model (`--config`, `NER_CONFIG_PATH`, `--show-config`).
- Model survey in `ner-detector/docs/models.md` (HF, GitHub, Context7 research).
- **99%** test coverage with **≥95%** gate; ML paths tested via mocks (no CI model download).
- Workspace entry in `marfago-labs.code-workspace` and root README project table.

## Docs

- `docs/architecture.md`, `cli.md`, `python-api.md`, `configuration.md`, `models.md`

## Benchmark framework

- `ner_detector/eval/` — gold JSONL loaders, strict/relaxed span F1, multi-backend runner, Markdown report.
- `benchmark/datasets/marfago_gold.jsonl`, `conll_dev_sample.jsonl`
- `uv run python benchmark/run_benchmark.py --pattern-only` (CI-friendly) or full ML compare.

## Next

- Optional: wire `ner-detector` into `text-compressor` entity F1 as a shared dependency.
- Publish repo when ready; nightly benchmark with real `transformers` + `gliner` weights.
