# Agno per-document sessions for synthetic LLM

**Archived dev note** (2026-05-27). Superseded by the public series post [The model wrote the entity. The offset was fiction.](../posts/05-entity-first-gold.md).

## Done

- Synthetic `engine: llm` defaults to `agent_mode: per_doc`: one Agno session per document with validation feedback turns (provider vs validation retry budgets).
- All LLM prompts live under `configs/` (`synthetic_llm_*.txt`, `labeling_*.txt`); Python only renders templates.
- Multi-genre document profiles (`scientific_abstract`, `news_article`, `blog_post`, `transcript`) with example configs and `synthetic_meta.document_type`.
- Structured logging to `gold.log` (Agno turns, stateless parity, `NER_GOLD_LOG_LEVEL`).
- Test suite: **240 passed**, coverage **95.26%** (`--cov-fail-under=95`).

## Next

- Optional: run a small real OpenRouter batch with `configs/synthetic_mixed.json` and review `gold.log` turn history.
- Add `sqlalchemy` to the `llm` extra if we want integration tests that call `create_synthetic_agent` without mocks.
